<odoo>
  <data>
    <!-- Vista de formulario para Publicación -->
    <record id="agenda_tipopublicacion_form_view" model="ir.ui.view">
      <field name="name">agenda.tipopublicacion.form</field>
      <field name="model">agenda.tipopublicacion</field>
      <field name="arch" type="xml">
        <form string="Publicación">
          <group>
            <field name="name"/>
          </group>
        </form>
      </field>
    </record>

    <!-- Vista de lista para Publicación -->
    <record id="agenda_tipopublicacion_list_view" model="ir.ui.view">
      <field name="name">agenda.tipopublicacion.list</field>
      <field name="model">agenda.tipopublicacion</field>
      <field name="arch" type="xml">
        <list string="tipopublicaciones">
          <field name="name"/>
        </list>
      </field>
    </record>

    <!-- Acción para abrir las tipopublicaciones -->
    <record  model="ir.actions.act_window" id="agenda.tipopublicacion_action_window">
      <field name="name">Tipos de publicaciones</field>
      <field name="res_model">agenda.tipopublicacion</field>
      <field name="view_mode">list,form</field>
    </record>
  </data>
</odoo>
